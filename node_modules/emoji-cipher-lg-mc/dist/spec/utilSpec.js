"use strict";

var _emojiCipher = require("../emoji-cipher");

describe("reverseObject Function", function () {
  it("check that key and value are flipped for a single pair object", function () {
    expect((0, _emojiCipher.reverseObject)({ a: 1 })).toEqual({ 1: "a" });
  });
  it("works for an empty object", function () {
    expect((0, _emojiCipher.reverseObject)({})).toEqual({});
  });
});
//# sourceMappingURL=utilSpec.js.map